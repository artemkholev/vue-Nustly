export enum Links {
  HOME = '/',
  ABOUT = '/about',
  BUCKET = '/bucket',
  ORDERS = '/orders',
  FAVORITES = '/favorites'
}

export enum PathNames {
  HOME = 'home',
  ABOUT = 'about',
  BUCKET = 'bucket',
  ORDERS = 'orders',
  FAVORITES = 'favorites'
}